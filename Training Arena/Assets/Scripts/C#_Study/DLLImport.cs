using System.Runtime.InteropServices;
using UnityEngine;

public class DLLImport : MonoBehaviour
{
    // How to interact with external libraries

    //[DllImport("user32.dll")] // its more complex than this, but this is the basic idea
    //public static extern bool MessageBeep(uint uType);


    //private void Start()
    //{
    //    MessageBeep(0);
    //}
}
