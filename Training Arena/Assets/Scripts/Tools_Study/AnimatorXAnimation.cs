using UnityEngine;

public class AnimatorXAnimation : MonoBehaviour
{
    // What is the difference, wich one use
    // for change the animation for legacy, we have to go on window -> Animation -> Animation
    // after create, click on the animation and active the debug mode, so you can set for legacy or not
    // if you have a single state animation, can be useful use the animation component

}
