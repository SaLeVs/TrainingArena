using UnityEngine;

public class Grids : MonoBehaviour
{
    // We can toggle grid and snap effect
    // if we press control can control tiny movement
    // we can snap objetcts automatically on grid, just open snap options
    // We have so much grid shortcuts for afacillit level design
    // We can change grid color
}
