using UnityEngine;

public class DecalsTip : MonoBehaviour
{
    // Decals are useful for print images on every surface, they addapt to the surface
    // we need to add a render freture to scene
    // after that we add a decal on rendering, decal - decal projector
    // we need to create a material of model shadergraphs/decals
    // and we can add a normal and a mask texture for these material
    // after that we can add a decal to the scene
}
