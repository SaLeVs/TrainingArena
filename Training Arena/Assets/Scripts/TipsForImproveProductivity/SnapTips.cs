using UnityEngine;

public class SnapTips : MonoBehaviour
{
    // vertex snapping is a technique that allows you to snap the vertices of a mesh to a grid
    // hold down V while moving the vertices to snap them to the grid
    // we can use shift + V to snap the vertices to the grid with a higher increment
    // we can increase the grid size and we have shortcuts for that

}
