using UnityEngine;

public class FrustumCullingTip : MonoBehaviour
{
    // Frustum culling its a script, so create a gameobject and add the component

    // disable auto catch camera and set the camera that will be used
    // Add a Frustum culling object for the object that will be culled

    // after build edge 
    // we can resize the edges and the object will be culled

    // its simple

    
}
