using UnityEngine;

public class ProfileAnalyzerTip : MonoBehaviour
{
    // Analyzes data across a range of frames
    // Monitors performance 
    // compare two sets of data
    // Its used for identify where optimization techniques are required
    // We need to instal the package from the package manager
    // this works alongside profiler so we need to open both
    // profiler is used for inspect individual frames
    // profile analyzer is used for inspect a range of frames and automatically prepares analytical data arranged in the sections we see here 
    // Change for self can show more data and specific things
    // we can see all threads
    // we can compare data, using compare and load and save some tests
    // 
}
